# Glenn's QR site
Website for the landing page of my QR codes. Deployed automatically using GHA and Github Pages.

## Development
Run the development server using `docker compose up` and access the website at `localhost:5173`. 
